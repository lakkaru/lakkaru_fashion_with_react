
const initialState = {};

export default function productCartReducer(state=initialState, action){
    switch (action) {
        case value:
            
            return '';
    
        default:
            return [...state];
    }

}